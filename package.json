{
  "name": "carbon-design-system-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:carbon-references": "bun scripts/sync-carbon-references.ts",
    "test:sync": "bash tests/sync-carbon-references/run.sh",
    "test:structural": "bash tests/structural/run.sh",
    "test:triggers": "bash tests/skill-triggering/run-all.sh",
    "test": "bun run test:structural && bun run test:triggers && bun run test:sync",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --check package.json tsconfig.json eslint.config.js scripts/**/*.ts README.md skills/*/references/source-index.md .prettierrc.json",
    "check": "bun run typecheck && bun run lint && bun run format && bun run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "@types/bun": "^1.2.13",
    "eslint": "^9.25.1",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.31.0"
  }
}
